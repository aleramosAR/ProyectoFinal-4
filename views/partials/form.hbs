<div class="col-md-8 mx-auto mb-5">
  {{getShortComment}}
  {{#if producto}}
    <form onsubmit="return actualizarProducto('{{producto._id}}')">
  {{else}}
    <form onsubmit="return crearProducto()">
  {{/if}}
    <fieldset class="card">
      {{#if producto}}
        <div class="card-header bg-success text-light">Actualizar el producto</div>
      {{else}}
        <div class="card-header bg-success text-light">Agregar un nuevo producto</div>
      {{/if}}
      <div class="card-body">
        <div class="row">
          <div class="mb-2 col-md-12">
            <label for="nombre" class="form-label">Titulo:</label>
            <input type="text" name="nombre" id="nombre" class="form-control" value="{{producto.nombre}}" />
          </div>
          <div class="mb-2 col-md-12">
            <label for="descripcion" class="form-label">Descripción:</label>
            <textarea name="descripcion" id="descripcion" class="form-control">{{producto.descripcion}}</textarea>
          </div>
          <div class="mb-2 col-md-6">
            <label for="categoria" class="form-label">Categoría:</label>
            <select name="categoria" id="categoria" class="form-control form-select" aria-label="Seleccione una categorîa">
              {{#select producto.categoria}}
              <option value="" selected disabled hidden>Seleccione una categoría</option>
                <option value="indumentaria">Indumentaria</option>
              <option value="accesorios">Accesorios</option>
              <option value="calzado">Calzado</option>
              {{/select}}
            </select>
          </div>
          <div class="mb-2 col-md-6">
            <label for="precio" class="form-label">Precio:</label>
            <input type="number" min="1" name="precio" id="precio" class="form-control" value="{{producto.precio}}" />
          </div>
          <div class="mb-2 col-md-6">
            <label for="codigo" class="form-label">Código:</label>
            <input type="number" min="1" name="codigo" id="codigo" class="form-control" value="{{producto.codigo}}" />
          </div>
          <div class="mb-2 col-md-6">
            <label for="stock" class="form-label">Stock:</label>
            <input type="number" min="1" name="stock" id="stock" class="form-control" value="{{producto.stock}}" />
          </div>
        </div>
        {{!-- <div class="row"> --}}
          <div class="mb-2">
          <input type="hidden" id="form" name="form" value="1" />
          {{#if producto}}
            <button class="btn btn-primary btn float-right mt-3">Actualizar producto</button>
            <a class="btn btn-danger text-white btn float-right mt-3 mr-2" onclick="location.href = '/productos'">Cancelar</a>
          {{else}}
            <button class="btn btn-primary btn float-right mt-3">Agregar producto</button>
          {{/if}}
          </div>
        {{!-- </div> --}}
      </div>
    </fieldset>
  </form>
</div>